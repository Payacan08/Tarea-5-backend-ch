<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
</head>
<body>
    <% if(length != 0) {%>
        <table>
            <tr>
                <th>Nombre</th>
                <th>Precio</th>
                <th>Imagen</th>
            </tr>
            <% contenedor.forEach(producto=>{ %>
                <tr>
                    <td><%=producto.title%></td>
                    <td><%=producto.price%></td>
                    <td><img src=<%=producto.thumbnail%> width="100" height="100"></td>
                </tr>
            <%})%>      
        </table>
    <%}else{%>
        <h3>No se encontraron Productos</h3>
    <%}%>
    <br>
    <br>
    <a type="button" href="/">Ingresar Producto</a>
</body>
<style>
    table, tr, th, td {border: 1px solid}
</style>
</html>